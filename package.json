{
  "name": "baby-bubble-game",
  "version": "1.0.0",
  "description": "Pop the Bubbles – a colourful baby game",
  "scripts": {
    "build": "node scripts/build.js",
    "icons": "node scripts/generate-icons.js",
    "sync": "npx cap sync",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "sharp": "^0.33.0"
  }
}
